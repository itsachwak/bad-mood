my funny moody web
Today I created a funny little website about my mood using the js instruction " document.getElementById "
I learned that the way you write it really matters — if you don’t write it correctly, it won’t work
The methods I practiced it was by using:
(style.display) → to show or hide elements on the page
(innerHTML) → to change the full HTML content inside an element (text, images, or other HTML)
I also learned that:
innerHTML is for replacing or adding new HTML content inside an element
style can change many things, like style.display, style.color, style.background, and much more. It all depends on what you want to change
and good luck everyone
